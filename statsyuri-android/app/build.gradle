plugins {
    id 'com.android.application'
}

android {
    namespace 'com.yugen.statsyuri'
    compileSdk 35

    defaultConfig {
        applicationId 'com.yugen.statsyuri'
        minSdk 24
        targetSdk 35
        versionCode 4
        versionName '0.4.0-safe'
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}

dependencies {
    // Platform Android UI only. No AppCompat, Google services, or Python runtime.
}
